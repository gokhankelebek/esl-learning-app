import React from 'react';
import { Container, Typography } from '@mui/material';

const PhraseMatching = () => {
  return (
    <Container>
      <Typography variant="h4">Phrase Matching</Typography>
      {/* Add implementation later */}
    </Container>
  );
};

export default PhraseMatching;
