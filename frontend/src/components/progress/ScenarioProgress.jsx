import React from 'react';
import { Container, Typography } from '@mui/material';

const ScenarioProgress = () => {
  return (
    <Container>
      <Typography variant="h4">Scenario Progress</Typography>
      {/* Add implementation later */}
    </Container>
  );
};

export default ScenarioProgress;
