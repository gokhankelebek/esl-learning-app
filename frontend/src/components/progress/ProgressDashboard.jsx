import React from 'react';
import { Container, Typography } from '@mui/material';

const ProgressDashboard = () => {
  return (
    <Container>
      <Typography variant="h4">Progress Dashboard</Typography>
      {/* Add implementation later */}
    </Container>
  );
};

export default ProgressDashboard;
